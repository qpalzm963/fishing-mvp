# Resolved on 2026-09-08 for the Windows x64 portable build.
# Keep this file in sync with runtime-manifest.json.  The project metadata
# intentionally keeps wider ranges for normal development; the portable
# artifact must use one repeatable native dependency set.
numpy==2.5.3
opencv-python-headless==5.0.0.93
PyYAML==6.0.3
av==18.1.0
pytest==9.1.1
pyinstaller==6.13.0
